// ======================================================
// Helpers
// ======================================================

/**
 * Test if the recieved argument is a String.
 * @param {*} obj Object to be tested
 */
export const isString = obj => typeof obj === "string" || obj instanceof String;
